version: '3.9'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    depends_on:
      - redis

  worker:
    build: .
    command: ["python", "worker.py"]
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
